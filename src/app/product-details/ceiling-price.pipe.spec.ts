import { CeilingPricePipe } from './ceiling-price.pipe';

describe('CeilingPricePipe', () => {
  it('create an instance', () => {
    const pipe = new CeilingPricePipe();
    expect(pipe).toBeTruthy();
  });
});
